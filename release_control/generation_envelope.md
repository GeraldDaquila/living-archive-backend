# USE v334 Provider Envelope Control

The current v334 production state is preserved on `main` at the validated recommendation-fit baseline.

Provider preflight is currently failing before model execution because the fixed system/user envelope is approximately 4.6K characters before evidence is added. The next production change must reduce duplicated fixed generation instructions without reducing canonical evidence authority, recommendation authority, sovereignty, provenance, or the v334 vulnerable-experience boundary.

This file records the controlled next intervention so it is reviewed separately from production behavior changes.
