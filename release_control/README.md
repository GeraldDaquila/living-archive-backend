# USE v334 Release Control

This branch exists to establish a controlled promotion path for the current v334 production state.

No visitor-facing behavior is defined here. Production changes should be prepared, audited, and promoted from an explicit reviewed commit rather than written directly to `main` with a stale blob SHA.
